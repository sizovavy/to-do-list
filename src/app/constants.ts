import { filterTypes } from './filter-types.enum';
import { ToDoItems } from './to-do-item.type';

export const title = 'to-do-list';
export const emptyToDoItems: ToDoItems = [];
export const initialFormControlInputValue = '';
export const initialFilterType = filterTypes.all;